import React from "react";
import ReactDOM from "react-dom/client";
import CampusApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CampusApp />
  </React.StrictMode>
);
